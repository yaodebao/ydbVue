import ytCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
ytCheckboxGroup.install = function(Vue) {
  Vue.component(ytCheckboxGroup.name, ytCheckboxGroup);
};

export default ytCheckboxGroup;
