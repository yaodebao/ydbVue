import ytCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
ytCheckboxButton.install = function(Vue) {
  Vue.component(ytCheckboxButton.name, ytCheckboxButton);
};

export default ytCheckboxButton;
